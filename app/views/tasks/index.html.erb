<h1>My Tasks</h1>

<% @tasks.each do |task| %>
  <h2><%= link_to task.title, show_path(task) %></h2>
  <p><%= task.completed %></p>
  <%= link_to 'Destroy', tasks_del_path(task), method: :delete, data: { confirm: "Are you sure?" } %>

<% end %>

<%= link_to "Add a new task", new_path %>

